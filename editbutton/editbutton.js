// CROSS BUTTON PAGE WITH A 
    // TODOS LIST SECTION
        // AND A TODOS DIV
            // CREATE THE BODY IN THE DOM
                // ADD TODO LIST AND THEN A CROSS BUTTON

// load the window and the form input and list
window.addEventListener('load', () => {
    // Once the page is loaded
    // Create the form element added to the page using the id for the form
        // OR the actual list or p element
    const form = document.querySelector("#new-todo-form");
    // create the INPUT
    const input = document.querySelector("#new-todo-input");
    // create the todo list element
    const todo_element = document.querySelector("#todos");

    // NOW for the form
        // which is the input and the submit button
    form.addEventListener('submit', (e) => {

    })
});
